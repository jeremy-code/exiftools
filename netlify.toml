# SPA fallback - send all non-file, non-API requests to index.html
[[redirects]]
from   = "/*"
to     = "/_shell.html"
status = 200

[build]
publish = "apps/client/dist/client"
command = "turbo run build --filter @exiftools/client"
